dist: xenial
sudo: required

services:
    - docker

language: node_js

node_js:
    - "12"

cache:
    directories:
        - node_modules

addons:
    chrome: stable

script:
    - npm install
    - npm run test-ci
    - docker build .
